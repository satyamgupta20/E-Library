<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.glauniversityelibrary">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".xdwsw"></activity>
        <activity android:name=".McaActivity" />
        <activity android:name=".Mca2Activity" />
        <activity android:name=".DiplomaActivity" />
        <activity android:name=".PostGraduateActivity" />
        <activity android:name=".DoctorateActivity" />
        <activity android:name=".GraduateActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".LoginSecActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>